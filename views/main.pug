extends layout

block content
    #dev_menu
        ul
            li 
                a(href="/stack") Stack
            li
                a(href="/DI") Dev
            li 
                a(href="#") 미지정
    if user && user.id
        p(style="text-align:center;")='안녕하세요. '
            span.nick=user.nick 
            span= '님'